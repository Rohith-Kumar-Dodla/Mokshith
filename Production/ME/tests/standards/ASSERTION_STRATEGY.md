ASSERTION STRATEGY
==================

Purpose
-------
Define assertion granularity and use of validators.

Guidelines
- Prefer domain-level assertions (validators) over UI-only checks.
- Keep assertions explicit and provide helpful failure messages.
- Isolate flaky assertions and tag them for investigation.

